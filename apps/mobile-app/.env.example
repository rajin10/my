# Local dev (Expo Go). EAS build profiles set these in eas.json.
EXPO_PUBLIC_API_URL=https://api.mahannankhan.info
# redirect = Expo Go web OAuth; native = EAS builds (@react-native-google-signin)
EXPO_PUBLIC_AUTH_PROVIDER=redirect
