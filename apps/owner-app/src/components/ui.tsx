/**
 * Backward-compatible re-export barrel.
 * All components now live in ./ui/ (Shadcn-style, one file per component).
 * Existing imports (`from "../ui"`) continue to resolve here.
 */
export * from "./ui/index";
