{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.(ts|tsx|js|jsx|json)$' && ./node_modules/.bin/biome check --write \"$f\" 2>/dev/null; } || true",
            "statusMessage": "Biome auto-fix..."
          }
        ]
      }
    ]
  }
}
