/// <reference types="react-native-css/types" />

// Enables NativeWind's `className` prop on React Native host components for the
// shared component library. Mirrors the per-app `nativewind-env.d.ts`; required
// once components style via `className` (#63) rather than inline styles (#62).
